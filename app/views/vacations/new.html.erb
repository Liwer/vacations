<% flash.each do |key, value| %>
    <div class="<%= flash_class(key) %>">
        <%= value %>
	</div>
<% end %>
<section class="newVacation">
    <div class="newInnerWrapper">
        <h1>New vacation</h1>
        <%= render 'form', vacation: @vacation %>
    </div>
</section>